"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const transform_utils_1 = require("./transform-utils");
describe('parseCSV', () => {
    it('should use the default values if no raw value is provided', () => {
        const result = transform_utils_1.parseCSV(undefined, ['default', 'values']);
        expect(result).toEqual(`'default', 'values'`);
    });
    it('should not add a comma when the input is not a csv list', () => {
        const result = transform_utils_1.parseCSV('test');
        expect(result).toEqual(`'test'`);
    });
    it('should return a csv string', () => {
        const result = transform_utils_1.parseCSV('foo,bar');
        expect(result).toEqual(`'foo', 'bar'`);
    });
});
//# sourceMappingURL=transform-utils_spec.js.map